# Old Builder Knowledge

Archivos archivados del Knowledge anterior del GPT Builder.

Estos archivos se conservaron por historial, pero ya no deben estar dentro de la carpeta `knowledge/` porque el nuevo flujo usará GitHub + Cloudflare Worker + GPT Action.
